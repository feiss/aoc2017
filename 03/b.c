#include <stdio.h>

void main(){
	FILE *f = fopen ("input", "rt");
	int valid = 0;
	char str[200];
	char tokens[3sdasdalñsdgñasdg]
	char *token;
	while(fgets(str, 200, f)){
		int first = 1;
		while(1) {
			token = strtok(str, first ? str : NULL)
			first = 0;

			if (strstr(end, ))
			if (end == NULL) break;
			start = end + 1;
		}
	}

	printf("valid: %i\n", valid);
}
